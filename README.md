# Poker
*holeCards list or tuple
*commonCards list
*smth to store the poker hands with rank and stuff
*selfHandEval function that, given holeCards and commonCards, will output the probability of eventually getting the hand for every winning hand
*elseHandEval function that, given holeCards and commonCards and number of players, will output the probability of any other person having a winning hand for every winning hand
*playerList holding the player names, in order, such that index 0 is first person to play
*selfVar = your name in player list
*gameHistList 2D list where each element is a list representing a player turn holding who, move, and value (if raise, otherwise is -1 if n/a)
*moves list
*function that takes the output of selfHandEval and output of elseHandEval and game history
*this function 
